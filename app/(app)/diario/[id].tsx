import { Text, View } from 'react-native';

// Placeholder — Tela 14 (detalhe do registro + revisões vinculadas).
export default function DetalheRegistro() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-lg text-text">Registro</Text>
    </View>
  );
}
