import { Text, View } from 'react-native';

// Placeholder — Telas 12 (lista) + 13 (calendário).
export default function Diario() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-2xl font-bold text-text">Diário</Text>
    </View>
  );
}
