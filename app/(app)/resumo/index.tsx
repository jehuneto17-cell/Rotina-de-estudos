import { Text, View } from 'react-native';

// Placeholder — Telas 9 (resumo), 15 (dashboard), 16 (relatório), 17 (evolução), 18 (metas).
export default function Resumo() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-2xl font-bold text-text">Resumo</Text>
    </View>
  );
}
