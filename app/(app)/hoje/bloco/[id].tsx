import { Text, View } from 'react-native';

// Placeholder — Tela 4 (editar bloco). Modal no mobile, drawer na web (USER-FLOWS §2).
export default function EditarBloco() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-lg text-text">Editar bloco</Text>
    </View>
  );
}
