import { Text, View } from 'react-native';

// Placeholder — Tela 4 (novo bloco).
export default function NovoBloco() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-lg text-text">Novo bloco</Text>
    </View>
  );
}
