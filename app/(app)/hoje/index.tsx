import { Text, View } from 'react-native';

// Placeholder — Telas 3 (Grade) + 20 (Foco do dia) do UI-SPEC.md.
export default function Hoje() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-2xl font-bold text-text">Hoje</Text>
    </View>
  );
}
