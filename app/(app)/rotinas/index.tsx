import { Text, View } from 'react-native';

// Placeholder — Telas 6 (rotinas) + 7 (tarefas avulsas).
export default function Rotinas() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-2xl font-bold text-text">Rotinas</Text>
    </View>
  );
}
