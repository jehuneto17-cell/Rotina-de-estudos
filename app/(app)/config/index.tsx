import { Text, View } from 'react-native';

// Placeholder — Tela 10 (configurações).
export default function Config() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-2xl font-bold text-text">Configurações</Text>
    </View>
  );
}
