import { Text, View } from 'react-native';

// Placeholder — Telas 21 (convidar) + 24 (gerenciar vínculos).
export default function Compartilhar() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-lg text-text">Compartilhar progresso</Text>
    </View>
  );
}
