import { Text, View } from 'react-native';

// Placeholder — Tela 23 (dashboard somente-leitura de um vínculo).
export default function ProgressoVinculo() {
  return (
    <View className="flex-1 bg-bg px-4 pt-12">
      <Text className="text-lg text-text">Progresso do vínculo</Text>
    </View>
  );
}
